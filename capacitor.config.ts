import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ApexUnited.SmartIN',
  appName: 'SmartInLite',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
